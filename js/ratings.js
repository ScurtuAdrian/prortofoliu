function collect_ratings(){
  let  ratings = {
    count : "0" ,
    sum : "0" , 
    average : "0" 
  }
  let reting= 0;

};

const elements =document.querySelectorAll('.rating');

array.forEach(element => {
    replace(element.id.star) ='';
});